apiVersion: 1

# 同 prometheus.yaml：删掉旧的按名自动生成条目（uid=loki），改用固定 uid
deleteDatasources:
  - name: Loki
    orgId: 1

datasources:
  - name: Loki
    type: loki
    uid: geesun-loki         # 看板 JSON 以此 uid 引用，勿随意改
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
