# 构建上下文为 /d/workspace（与 langchain-cubesandbox 同级）。
# 仅排除「不进镜像」的兄弟仓库与临时产物，减小 context tar；
# geesun_agent 与 langchain-cubesandbox 必须保留（Dockerfile 显式 COPY）。
# 注意：此文件需位于构建上下文根 /d/workspace/.dockerignore，
#       deploy/build-push.sh 会在构建前自动把它复制到该位置。

# 兄弟仓库（不进镜像）
langfuse
geesun_agent_web

# VCS / 缓存 / 编辑器
.git
**/.git
**/__pycache__
**/*.pyc
**/.workbuddy
**/.idea
**/node_modules

# 运行时临时产物（不进构建）
**/server.log
**/reports
**/certs
**/.env

# 宿主机 Python 虚拟环境（不应进镜像；Dockerfile 用 uv sync 从 uv.lock 装依赖）
**/.venv
**/venv

# 非 agent 镜像所需的兄弟仓库（各有独立构建；减小 agent 构建上下文）
geesun_mcp_server
